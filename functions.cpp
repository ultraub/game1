#include <iostream>
#include <fstream>
#include <string>
using namespace std;

void readData (FILE * inFile) {
  if (inFile.is_open()) {
    // Read from file and put in object that understands the data
    }
}

void saveData (FILE* inFile) {
    // Writes to file with whatever is inside the object so it can be read later
}
   
  
